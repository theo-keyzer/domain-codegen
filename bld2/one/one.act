// -----------------------------
Actor { name = main }

All { what = Ops actor = all }

// -----------------------------
Actor { name = all }

C { desc = "hi ${ops_id} x ${data}" }

Its { what = data actor = try }

// -----------------------------
Actor { name = try }

C { desc = "${._type} ${.} good" }

This { path = . actor = try2 }

// -----------------------------
Actor { name = try2 }

C { desc = "${._type} ${._key} ${.}" }

This { path = . actor = try3 }

// -----------------------------
Actor { name = try3 }

C { desc = "${._type} ${._key} ${.}" }

